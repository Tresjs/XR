<script setup lang="ts">
/* import { inject } from 'vue'

const state = inject('state') */
/* const {
  controllers,
  interactions,
  hoverState,
  hasInteraction,
  getInteraction,
} = state */

//    const events = useThree((state) => state.events)
//   const get = useThree((state) => state.get)
//   const raycaster = useThree((state) => state.raycaster)clear
</script>

<template>
  <TresGroup>
    <slot></slot>
  </TresGroup>
</template>
